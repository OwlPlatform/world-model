-- Table definition
CREATE TABLE Origins (
  idOrigin INTEGER AUTO_INCREMENT PRIMARY KEY,
  originName VARCHAR(170) NOT NULL) ENGINE = INNODB DEFAULT CHARACTER SET utf16 COLLATE utf16_unicode_ci;

-- Index
CREATE INDEX idx_origin_name ON Origins (originName);
